these are the topics we read in phase 0 of the course
Repository, commit, branch, push, merge
Container, image, Docker Compose, volume, healthcheck, profile
Endpoint, GET, POST, status code, JSON, multipart/form-data
Environment variable, .env file
Service name / internal networking (why http://qdrant:6333 works between containers)